{
  "name": "the-button",
  "version": "1.0.0",
  "private": "true",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' --elide-lines=0 dev",
    "lint": "bun run --filter '*' --elide-lines=0 lint",
    "build": "bun run --filter '*' --elide-lines=0 build",
    "typecheck": "bun run --filter '*' --elide-lines=0 typecheck"
  }
}

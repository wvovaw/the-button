{
  "name": "the-button",
  "version": "1.0.0",
  "private": "true",
  "type": "module",
  "scripts": {
    "dev": "pnpm --stream -r run dev",
    "lint": "pnpm --stream -r run lint",
    "build": "pnpm --stream -r run build",
    "format": "pnpm --stream -r run format",
    "typecheck": "pnpm --stream -r run typecheck",
    "compose:up": "podman-compose -f docker-compose.dev.yml up -d",
    "compose:down": "podman-compose -f docker-compose.dev.yml down"
  },
  "packageManager": "pnpm@9.12.3"
}
